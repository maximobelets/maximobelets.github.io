import s from './Header.module.scss';

export const Header = () => {
    return (
        <header className={s.root}>
            <h1 className={s.title}>
                TEST
            </h1>
        </header>
    )
}