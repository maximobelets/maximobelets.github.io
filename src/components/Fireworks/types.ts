export type Particle = {
    // current x position
    x: number;

    // current y position
    y: number;

    // current x velocity
    vx: number;

    // current y velocity
    vy: number;

    // how many frames particle will be alive
    life: number;

    // color of particle
    color: string;
};
